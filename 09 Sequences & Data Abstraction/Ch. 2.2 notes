2.2.2 Pairs
List 列，用方括号括住，用逗号分隔。
List中的元素有2种访问方式：
 （1）多重赋值：将List中的每个元素绑定到不同名称
 （2）元素选择运算符：同样用方括号表示，跟在某个表达式（List名）后的方括号中加上序号（0-indexed），可取出其值
 （3）元素选择运算符的等效函数为getitem(List名，序号)
 
2.2.3   Abstraction Barriers
抽象壁垒（或抽象层级，暂不确定如何翻译）
-->首先需要介绍数据抽象
   数据抽象的基本思想是确定一组基本操作，根据这些操作将表达对某种值的所有操作，然后仅使用这些操作来操作数据。
-->抽象壁垒（抽象层级）更像编程的行为准则，如果不这样做不会产生程序上的报错，但是会影响其成为“优美的程序”，更会影响程序的维护和优化。
-->抽象壁垒（抽象层级）可以表示为以下例子：
