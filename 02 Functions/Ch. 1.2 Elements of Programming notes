1. Expressions:
  1.1 primitive expressions:numbers/ numerals
  1.2 compound expressions: expressions representing numbers may be combined with mathematical operators to form a compound expression
      1.2.1 call expression: it applies a function to some arguments
      1.2.2 nested expression 
2. Expression Tree

3. The Non-Pure Print Function
    3.1 Pure functions
        Pure functions have the property that applying them has no effects beyond returning a value. 
        >>>abs(-2） 
    3.2 Non-pure functions
        In addition to returning a value, applying a non-pure function can generate side effects, which make some change to the state of the interpreter or computer.
        >>>print(-2)
        The value that print returns is always None.
